// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` (production) replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.
//
// API keys are intentionally NOT stored here. Alpha Vantage and Finnhub calls go
// through serverless proxy functions (netlify/functions/*) that keep the keys
// server-side. See README.md.

export const environment = {
  production: false,
};
